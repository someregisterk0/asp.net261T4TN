@{
    Layout = "_LayoutAjax";
}

<script>
    var $ = {};
    $.get = function (url, f) {
        var xhr = new XMLHttpRequest();
        xhr.open('get', url);
        xhr.send();
        xhr.onload = function () {
            console.log('Ham tu viet');
            console.log(this.response);
            console.log(xhr.response);

            f.call(this, this.response);
            //f(this.response);
        };
    };
    $.get('/ajax/categories', function (a) {
        console.log(this.response);
        console.log(a);
    });

    func = function (a, b) {
        return a + b;
    };
    console.log(func(1, 2));




    var xhr = new XMLHttpRequest();
    xhr.open('get', '/ajax/categories');
    xhr.send();
    xhr.onload = function () {
        console.log(this.response);
    };

    var id = 1;
    var name = "teo";
    var xhr = new XMLHttpRequest();
    xhr.open('post', '/ajax/CategoryById');
    xhr.setRequestHeader('content-type', 'application/x-www-form-urlencoded');
    xhr.send('id=' + id + "&name=" + name);
    xhr.onload = function () {
        console.log(this.response);
    };

    //$.post('/ajax/CategoryById', { id: 1, name: "teo" }, function (o) {
    //    console.log(o);
    //});

    var xhttp = new XMLHttpRequest();
    xhttp.onreadystatechange = function () {
        if (this.readyState == 4 && this.status == 200) {
            console.log(this.response);
        }
    };
    xhttp.open("GET", "/ajax/categories", true);
    xhttp.send(); 
</script>
